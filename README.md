# 📘 راهنمای راه‌اندازی سیستم مدیریت فروشگاه تلگرام (TeleShop Admin)

به سیستم مدیریت فروشگاه تلگرام خوش آمدید. این اپلیکیشن یک پنل مدیریت کامل برای مدیریت محصولات، دسته‌بندی‌ها و ارسال خودکار پست‌ها به کانال تلگرام با استفاده از توضیحات تولید شده توسط هوش مصنوعی است.

---

## 🚀 شروع سریع

### ۱. ورود به سیستم
برای ورود اولیه به پنل مدیریت، از اطلاعات پیش‌فرض زیر استفاده کنید:

- **نام کاربری:** `admin`
- **رمز عبور:** `123`

*(نکته: می‌توانید کاربران جدید را از طریق دکمه "ثبت نام" در صفحه ورود اضافه کنید.)*

---

## 🤖 تنظیمات ربات تلگرام

برای اتصال فروشگاه به کانال تلگرام خود، مراحل زیر را دنبال کنید:

1.  **ساخت ربات:**
    - در تلگرام وارد ربات [@BotFather](https://t.me/BotFather) شوید.
    - دستور `/newbot` را ارسال کنید.
    - یک نام و یک نام کاربری (که با `bot` تمام شود) برای ربات خود انتخاب کنید.
    - **توکن ربات (Bot Token)** را کپی کنید.

2.  **تنظیم کانال:**
    - یک کانال در تلگرام بسازید (یا از کانال موجود استفاده کنید).
    - ربات خود را به عنوان **Administrator (مدیر)** در کانال اضافه کنید تا اجازه ارسال پیام داشته باشد.

3.  **دریافت شناسه کانال (Chat ID):**
    - شناسه کانال‌های عمومی همان نام کاربری کانال با علامت `@` است (مثلاً `@my_shop`).
    - برای کانال‌های خصوصی، می‌توانید یک پیام از کانال را به ربات‌هایی مثل `@userinfobot` فوروارد کنید تا `ID` عددی را دریافت کنید.

4.  **اتصال در پنل:**
    - در پنل مدیریت به بخش **تنظیمات ربات** بروید.
    - توکن و شناسه کانال را وارد کرده و ذخیره کنید.

---

## ✨ تنظیمات هوش مصنوعی (Gemini AI)

این برنامه از هوش مصنوعی گوگل (Gemini) برای نوشتن توضیحات جذاب محصولات استفاده می‌کند.
برای فعال‌سازی، نیاز به `API Key` دارید که در تنظیمات محیطی (Environment Variables) برنامه قرار می‌گیرد.

1.  در صفحه افزودن محصول، پس از وارد کردن نام و دسته‌بندی، روی دکمه **"✨ تولید با هوش مصنوعی"** کلیک کنید تا توضیحات به صورت خودکار نوشته شود.

---

## 📦 ویژگی‌های کلیدی

- **مدیریت محصولات:** افزودن، ویرایش و حذف محصولات با تصویر و قیمت.
- **ارسال به تلگرام:** ارسال عکس و کپشن محصول به کانال تنها با یک کلیک.
- **مدیریت دسته‌بندی‌ها:** سازماندهی محصولات در دسته‌های مختلف.
- **حالت شب (Dark Mode):** پشتیبانی کامل از تم تاریک و روشن.
- **داشبورد مدیریتی:** مشاهده آمار کلی فروشگاه.
- **فیلتر پیشرفته:** جستجو در محصولات بر اساس قیمت و دسته.

---

## 🛠 نکات فنی

- **ذخیره‌سازی:** تمام اطلاعات (به جز تصاویر سنگین که به Base64 تبدیل می‌شوند) در `LocalStorage` مرورگر شما ذخیره می‌شوند. با پاک کردن حافظه مرورگر، اطلاعات پاک خواهند شد.
- **تصاویر:** برای عملکرد بهتر، از تصاویر با حجم مناسب (زیر ۱ مگابایت) استفاده کنید.

---

## 🌐 راهنمای استقرار در سرور (Deployment)

از آنجایی که این پروژه یک **Single Page Application (SPA)** است، می‌توانید آن را روی هر وب‌سروری که فایل‌های استاتیک (HTML, CSS, JS) را سرو می‌کند، قرار دهید.

### پیش‌نیاز: ساخت نسخه نهایی (Build)
قبل از آپلود روی سرور، باید پروژه را بیلد بگیرید تا فایل‌های بهینه شده تولید شوند. در ترمینال دستور زیر را وارد کنید:

```bash
npm run build
```

پس از اتمام، یک پوشه به نام `dist` یا `build` ایجاد می‌شود. محتویات **داخل** این پوشه را باید روی سرور آپلود کنید.

---

### روش اول: هاست‌های ابری (Vercel / Netlify) - پیشنهادی
ساده‌ترین روش، استفاده از سرویس‌های رایگان مثل Vercel است:
1. پروژه را در GitHub آپلود کنید.
2. در سایت Vercel اکانت بسازید و "Add New Project" را بزنید.
3. مخزن گیت‌هاب خود را انتخاب کنید.
4. دکمه Deploy را بزنید. تمام تنظیمات به صورت خودکار انجام می‌شود.

---

### روش دوم: سرور شخصی (VPS Linux / Ubuntu) با Nginx

اگر می‌خواهید روی سرور لینوکس خودتان برنامه را اجرا کنید:

#### ۱. نصب Nginx
```bash
sudo apt update
sudo apt install nginx
```

#### ۲. آپلود فایل‌ها
محتویات پوشه `dist` (که در مرحله بیلد ساختید) را به مسیر `/var/www/teleshop` در سرور منتقل کنید.

#### ۳. تنظیمات Nginx
فایل تنظیمات جدید بسازید:
```bash
sudo nano /etc/nginx/sites-available/teleshop
```

محتویات زیر را در آن قرار دهید (دامین خود را جایگزین `your_domain.com` کنید):

```nginx
server {
    listen 80;
    server_name your_domain.com;
    root /var/www/teleshop;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```
*نکته مهم:* خط `try_files $uri $uri/ /index.html;` برای برنامه‌های React ضروری است تا روتینگ به درستی کار کند و با رفرش صفحه ارور 404 نگیرید.

#### ۴. فعال‌سازی و ریستارت
```bash
sudo ln -s /etc/nginx/sites-available/teleshop /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

اکنون سایت شما در دسترس است!